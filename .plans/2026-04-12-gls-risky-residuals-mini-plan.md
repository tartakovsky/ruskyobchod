# 2026-04-12 10:30:00 CET

## selected next block

Risky residual proof work inside:

- `wordpress/wp-content/plugins/gastronom-lang-switcher/gastronom-lang-switcher.php`

This is the chosen next block after:

- completed safe bounded-refactor phase
- handoff current-state refresh
- retained surface classification

## why this block

This is the highest-signal next step because:

- the remaining blast radius is now concentrated in retained fallback/output cleanup logic
- at least one extraction attempt in the empty-cart shell already proved this zone can regress live output
- continuing generic helper extraction would violate the stop-line

## objective

Do not remove residual logic yet.

First prove exact output dependency for small subsets inside the retained surface.

## execution rule

For this mini-phase, no change is allowed unless the exact affected output is captured before and after.

Required proof style:

1. identify one narrow phrase group or one narrow cleanup behavior
2. capture exact live output for the affected route
3. test removal or narrowing locally
4. compare exact output
5. only then decide `keep` or `remove later`

## first candidate slices

### candidate 1. cookie notice residual normalization on server-rendered cart/checkout shell

Reason:

- it is visible
- it is already covered by baseline checks
- it previously exposed exact-output dependency when the wrong area was changed

### candidate 2. footer/legal residual normalization in server-rendered shell

Reason:

- exact phrases are known
- baseline checks already cover them
- this is a good proof-oriented target if handled as phrase groups, not broad cleanup

### candidate 3. empty-cart shell title cleanup

Reason:

- high-value
- already proven risky

Constraint:

- touch last, not first

## explicit non-goals

- no new broad abstraction over retained output cleanup
- no multi-surface live deploy
- no mixed work across translation, Dotypos, checkout UX, and styling

## completion condition for this mini-plan

The mini-plan is successful when at least one retained slice is proven as:

- `keep`
or
- `remove later`

with exact-output evidence, not intuition.
