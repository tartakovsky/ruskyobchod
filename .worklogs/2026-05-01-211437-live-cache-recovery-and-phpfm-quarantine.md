## Context

Live `ruskyobchod.sk` mobile performance work exposed a broken `WP Super Cache` layer and a malicious PHP file manager payload in `wp-content/cache`.

## Actions

1. Confirmed a malicious `PHP File manager ver 1.4` payload in:
   - `wp-content/cache/dofault.php`
   - `wp-content/wefile.php`
   - top-level `deals1.php`
   - inactive plugin-like dirs `wp-content/plugins/er-deals` and `wp-content/plugins/wp-solo`
2. Moved those files/directories out of web root into:
   - `/home/u595644545/quarantine-rusky-20260501-phpfm`
3. Verified storefront boundaries after quarantine:
   - home `200`
   - `?lang=ru` home `200`
   - cart `200`
   - checkout empty-cart redirect remained correct
   - `wp-login.php` remained `200`
   - known malicious URLs returned `404`
4. Audited live `WP Super Cache` state and found it half-enabled:
   - `WP_CACHE` bootstrap present
   - DB options were off
   - `wp-cache-config.php` still had `$cache_enabled = false;`
   - `wp-content/cache/supercache` did not exist
5. Enabled cache options in WordPress and then synchronized the real phase1 config with a narrow backed-up live edit:
   - backup: `wp-content/wp-cache-config.php.bak-20260501-2115`
   - changed only `$cache_enabled = false;` to `$cache_enabled = true;`
6. Warmed the homepage and confirmed cache output:
   - homepage source ended with `Cached page generated by WP-Super-Cache`
   - cache file created at `wp-content/cache/supercache/ruskyobchod.sk/index-https.html`

## Result

- Cache layer is now actually active for the plain homepage.
- Server-side warm-request timing improved materially on homepage:
  - first request TTFB about `0.494s`
  - second request TTFB about `0.128s`
- Fresh mobile trace on `/?lang=ru` showed:
  - `LCP 519ms`
  - `TTFB 149ms`
  - remaining largest subpart: `render delay 255ms`

## Remaining Work

Do not resume blind JS surgery. The next safe optimization frontier is narrower:

1. Re-measure mobile PSI after cache warmup.
2. If more gain is needed, inspect homepage CSS stack (`style.css`, `bootstrap.css`, `block-frontend.css`, `blocks.css`, `gls-style.css`) before touching storefront behavior.
3. Keep checkout/cart/payment/delivery/weight-order logic out of scope unless explicitly re-verified.
